/*  NODE *ptr = *head;
    NODE *ptr1 = *head;

    if( (*head)==NULL||(*head)->next==NULL )
    {
        return NULL;
    }

    while(ptr1 !=NULL && ptr1->next!=NULL)
    {

        ptr=ptr->next;
        ptr1=ptr1->next->next;
    }
    return ptr;
    */